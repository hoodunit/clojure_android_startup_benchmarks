## Clojure on Android Startup Benchmarks

This contains a tool for benchmarking Android app start times (/benchmarker), a few benchmark programs used (/programs), and the logs from some benchmarking runs (/logs).
